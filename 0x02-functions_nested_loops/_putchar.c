#include <stdio.h>
/**main - prints _putchar, followed by a new line.
 * Return: 0 when successful
 * Descripton - The program should return 0
 */

int main (void)
{
char s[] = "_putchar";

int i = 0;
while (s[] = != '\0')
{
_putchar(s[i]);
i++;
}
_putchar(10);
return (0);
}
